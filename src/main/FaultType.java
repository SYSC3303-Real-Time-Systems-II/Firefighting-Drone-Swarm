/**
 * This is an enum type for the types of faults that can occur to the drone such as not arriving on time,
 * the nozzle door getting stuck and corrupted packet.
 */

public enum FaultType {
    STUCK, JAMMED, CORRUPT
}
